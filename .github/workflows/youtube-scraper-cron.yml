# Cào video YouTube 2 lần/ngày (05:00 và 17:00 ICT) — chạy TRỰC TIẾP trên GitHub runner (không gọi Render).
# Lý do: tránh timeout / cold-start Render free khi scrape nhiều kênh + comment.
#
# BẮT BUỘC — Settings → Secrets and variables → Actions → Variables:
#   DB_HOST, DB_DATABASE, DB_USERNAME, DB_PASSWORD
#   YOUTUBE_API_KEY
#   MAIL_HOST, MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM_ADDRESS, MAIL_MAIN
#
# Tuỳ chọn Variables:
#   MAIL_PORT (mặc định 587), MAIL_ALERT_BCC (BCC khi báo lỗi dịch vụ / alert)
#   DB_PORT (mặc định 19437), DB_SSL (mặc định true)
#   YOUTUBE_CHANNEL_IDS (vd. 1,2,3 — trống = tất cả kênh youtube trong DB)
# Limit cào lấy từ channels.max_posts / max_top_comments / max_replies
#
# Chạy: Actions → "YouTube Scraper Cron" → Run workflow

name: YouTube Scraper Cron

on:
  schedule:
    # 05:00 và 17:00 ICT (UTC+7) = 22:00 và 10:00 UTC. GitHub có thể trễ vài phút so với đúng giờ.
    - cron: '0 22 * * *'
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      channel_ids:
        description: 'Chỉ cào một số channel_id (vd. 1,2,3 — trống = tất cả)'
        required: false
        type: string

jobs:
  scrape-youtube:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: backend-express

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: backend-express/package-lock.json

      - name: Check required variables
        env:
          DB_HOST: ${{ vars.DB_HOST }}
          DB_DATABASE: ${{ vars.DB_DATABASE }}
          DB_USERNAME: ${{ vars.DB_USERNAME }}
          DB_PASSWORD: ${{ vars.DB_PASSWORD }}
          YOUTUBE_API_KEY: ${{ vars.YOUTUBE_API_KEY }}
          MAIL_HOST: ${{ vars.MAIL_HOST }}
          MAIL_USERNAME: ${{ vars.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ vars.MAIL_PASSWORD }}
          MAIL_FROM_ADDRESS: ${{ vars.MAIL_FROM_ADDRESS }}
          MAIL_MAIN: ${{ vars.MAIL_MAIN }}
        run: |
          set -euo pipefail
          missing=()
          for key in DB_HOST DB_DATABASE DB_USERNAME DB_PASSWORD YOUTUBE_API_KEY \
            MAIL_HOST MAIL_USERNAME MAIL_PASSWORD MAIL_FROM_ADDRESS MAIL_MAIN; do
            if [ -z "${!key:-}" ]; then
              missing+=("$key")
            fi
          done
          if [ "${#missing[@]}" -gt 0 ]; then
            echo "::error::Missing repository variables: ${missing[*]}"
            echo "Add them at: Settings → Secrets and variables → Actions → Variables"
            echo "Copy values from backend-express/.env (DB_* / YOUTUBE_API_KEY / MAIL_*)."
            exit 1
          fi
          echo "All required variables are present."

      - name: Install dependencies
        run: npm ci

      - name: Run YouTube scraper
        env:
          NODE_ENV: production
          DB_CONNECTION: mysql
          DB_HOST: ${{ vars.DB_HOST }}
          DB_PORT: ${{ vars.DB_PORT || '19437' }}
          DB_DATABASE: ${{ vars.DB_DATABASE }}
          DB_USERNAME: ${{ vars.DB_USERNAME }}
          DB_PASSWORD: ${{ vars.DB_PASSWORD }}
          DB_SSL: ${{ vars.DB_SSL || 'true' }}
          YOUTUBE_API_KEY: ${{ vars.YOUTUBE_API_KEY }}
          MAIL_MAILER: smtp
          MAIL_HOST: ${{ vars.MAIL_HOST }}
          MAIL_PORT: ${{ vars.MAIL_PORT || '587' }}
          MAIL_USERNAME: ${{ vars.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ vars.MAIL_PASSWORD }}
          MAIL_ENCRYPTION: tls
          MAIL_FROM_ADDRESS: ${{ vars.MAIL_FROM_ADDRESS }}
          MAIL_MAIN: ${{ vars.MAIL_MAIN }}
          MAIL_ALERT_BCC: ${{ vars.MAIL_ALERT_BCC }}
          YOUTUBE_CHANNEL_IDS: ${{ inputs.channel_ids || vars.YOUTUBE_CHANNEL_IDS }}
        run: npm run app:youtube-scrape

# ---------------------------------------------------------------------------
# [DEPRECATED] Gọi API Render — giữ để tham chiếu, không dùng (tránh cold-start / timeout).
#
# Variable cũ: API_BASE_URL (vd. https://your-api.example.com/api)
#
#      - name: Fetch YouTube channel IDs and run scraper
#        env:
#          API_BASE_URL: ${{ vars.API_BASE_URL }}
#        run: |
#          set -euo pipefail
#
#          if [ -z "${API_BASE_URL:-}" ]; then
#            echo "::error::Missing repository variable API_BASE_URL (e.g. https://host/api)"
#            exit 1
#          fi
#
#          API_BASE_URL="${API_BASE_URL%/}"
#
#          python3 <<'PY'
#          import json
#          import os
#          import sys
#          import urllib.error
#          import urllib.parse
#          import urllib.request
#
#          base = os.environ["API_BASE_URL"].rstrip("/")
#          timeout = 1800
#
#          def request(method, path, body=None):
#              url = f"{base}{path}"
#              data = None
#              headers = {"Accept": "application/json"}
#              if body is not None:
#                  data = json.dumps(body).encode("utf-8")
#                  headers["Content-Type"] = "application/json"
#              req = urllib.request.Request(url, data=data, headers=headers, method=method)
#              try:
#                  with urllib.request.urlopen(req, timeout=timeout) as resp:
#                      raw = resp.read().decode("utf-8")
#                      return resp.status, json.loads(raw) if raw else {}
#              except urllib.error.HTTPError as e:
#                  raw = e.read().decode("utf-8", errors="replace")
#                  try:
#                      payload = json.loads(raw) if raw else {}
#                  except json.JSONDecodeError:
#                      payload = {"raw": raw}
#                  print(f"::error::HTTP {e.code} {method} {url}: {payload}", file=sys.stderr)
#                  sys.exit(1)
#              except Exception as e:
#                  print(f"::error::{method} {url} failed: {e}", file=sys.stderr)
#                  sys.exit(1)
#
#          channel_ids = []
#          page = 1
#          per_page = 100
#
#          while True:
#              qs = urllib.parse.urlencode(
#                  {"type_channel": "youtube", "page": page, "per_page": per_page}
#              )
#              status, payload = request("GET", f"/channels?{qs}")
#              if status != 200 or payload.get("code") not in (None, 200):
#                  print(f"::error::Unexpected channels response: {payload}", file=sys.stderr)
#                  sys.exit(1)
#
#              data = payload.get("data") or {}
#              rows = data.get("result") or []
#              youtube_rows = [
#                  row
#                  for row in rows
#                  if str(row.get("type_channel") or "").strip().lower() == "youtube"
#              ]
#              for row in youtube_rows:
#                  cid = row.get("id")
#                  if cid is not None:
#                      channel_ids.append(int(cid))
#
#              pagination = data.get("pagination") or {}
#              total_pages = int(pagination.get("total_pages") or 1)
#              if page >= total_pages or not rows:
#                  break
#              page += 1
#
#          unique_ids = list(dict.fromkeys(channel_ids))
#          if not unique_ids:
#              print("No YouTube channels found — skip scraper run.")
#              sys.exit(0)
#
#          status, payload = request(
#              "POST",
#              "/scraper/youtube/run",
#              {"channel_id": unique_ids},
#          )
#          if status != 200 or payload.get("code") not in (None, 200):
#              print(f"::error::Scraper failed: {payload}", file=sys.stderr)
#              sys.exit(1)
#          print("Scraper OK:", json.dumps(payload.get("data") or {}, ensure_ascii=False))
#          PY
# ---------------------------------------------------------------------------
